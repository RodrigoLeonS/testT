const Loader = () => {
    return (
        <section className="loader loader--component" id="loader">
            <div className="loader__spinner"></div>
        </section>
    );
};

export default Loader;